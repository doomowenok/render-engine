# RenderEngine
Implementation of render engine using only CPU power.

# Notes for Windows
    1. Need to copy SDL2.dll to "C:\Windows\System32" folder.
    2. Boot function should be "int SDL_main(int argc, char *argv[])"